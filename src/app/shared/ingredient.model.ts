export class Ingredient {
    // public name: string;
    // public amount: number;

    //Alternative shortcut to define model properties
    constructor(public name: string, public amount: number){
        // this.name = name;
        // this.amount = amount;
    }
}